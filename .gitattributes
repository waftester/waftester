# Force LF line endings for files that must be Unix-compatible.
# Shell scripts break with CRLF on Linux/macOS.
# The JS shebang (#!/usr/bin/env node) fails with \r on Unix.

*.sh text eol=lf
npm/cli/bin/cli.js text eol=lf
