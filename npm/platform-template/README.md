# Platform Binary

This package contains the WAFtester binary for a specific platform.

**Do not install this package directly.** Install `@waftester/cli` instead:

```bash
npm install -g @waftester/cli
```

The correct platform binary is selected automatically via `optionalDependencies`.
